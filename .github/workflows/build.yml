name: Build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Build
        run: |
          cd handheld/project/iosproj
          xcodebuild -scheme minecraftpe -derivedDataPath build -destination 'generic/platform=iOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO
          mkdir -p Payload packages
          cp -R build/Build/Products/Debug-iphoneos/minecraftpe.app Payload/
          zip -r packages/minecraftpe.ipa Payload
          rm -rf Payload

      - name: Upload artifact
        uses: actions/upload-artifact@main
        with:
          name: artifact
          path: packages/*.ipa
